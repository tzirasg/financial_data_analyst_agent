# Normalize line endings to LF in the repo (avoids CRLF churn on Windows)
* text=auto eol=lf
*.png binary
*.tgz binary

# Keep GitHub's language stats focused on the source, not data/reports
prompts/exemplar_bank.json linguist-generated=true
results/*.json linguist-generated=true
